<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qna_boardMapper">

	<!-- 게시판 목록 조회 -->
    <select id="list" resultType="com.sarf.vo.Qna_BoardVO">
	   SELECT *	FROM QNA_BOARD
			ORDER BY BNO DESC
	</select>

	<!-- 게시판 글 작성 -->
 	<insert id="write" >
		INSERT INTO Qna_BOARD(BNO, SUBJECT, NAME, CONTENT)
		VALUES(Qna_BOARD_SEQ.NEXTVAL, #{subject}, #{name}, #{content})
	</insert> 

	<!-- 게시물 조회 -->
 	<select id="read" parameterType="int" resultType="com.sarf.vo.Qna_BoardVO">
		SELECT	BNO
			  , SUBJECT
			  , CONTENT
			  , NAME
			  , REGDATE
		 FROM A_BOARD
		 WHERE BNO = #{bno}
	</select>
	
	<!-- 게시물 삭제 -->
 	<delete id="delete" parameterType="int">
		DELETE 
		  FROM Qna_BOARD
		 WHERE BNO = #{bno}
	</delete>
		
</mapper>













